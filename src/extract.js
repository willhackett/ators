// @flow
import operators, { type operatorTypes } from './_operators'

type optionsType = {
  allowedOperators: [operatorTypes]
}

const extract = (input: string, context: Object = {}, options: optionType = {}) => {
  // Grouping Method
  const evaluationGroups = []


  // Parse Member Access {{ a.b.c[3] }}

  // Postfix Increment

  // Postfix Decrement

  // Math Ops

  // Comparisons

  // Evaluations


}


export default compare